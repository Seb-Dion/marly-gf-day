# Photos

Drop your real photos in here with these exact filenames and they'll
automatically replace the placeholders on the site:

- `hero.jpg` — the big photo at the top (portrait orientation looks best)
- `gallery-1.jpg` through `gallery-6.jpg` — the gallery grid

No code changes needed — just add the files.
